version: '3'
services:
  laravel-app:
    build:
      context: .
    depends_on:
      - redis
    ports:
      - "8080:80"
    dns:
      - 8.8.8.8
      - 8.8.4.4
    networks:
      - common-network
  redis:
    image: redis
    restart: always
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    env_file:
      - .env
    networks:
      - common-network
networks:
  common-network:
