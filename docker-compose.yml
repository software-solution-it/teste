version: '3'
services:
  laravel-app:
    build:
      context: .
    depends_on:
      - redis
    ports:
      - "3000:8080"
  redis:
    image: redis
    restart: always
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    env_file:
      - .env
